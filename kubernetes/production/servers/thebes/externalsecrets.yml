apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mc-rcon-creds
  namespace: minecraft
spec:
  secretStoreRef:
    kind: SecretStore
    name: minecraft-secret-store
  refreshPolicy: Periodic
  refreshInterval: "1h0m0s"
  target:
    name: thebes-mc-rcon-creds
    creationPolicy: Owner
  data:
    - secretKey: RCON_PASSWORD
      remoteRef:
        key: minecraft/servers/thebes/rcon
        property: password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mc-paper-global
  namespace: minecraft
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: minecraft-secret-store
  target:
    name: thebes-mc-paper-global
    creationPolicy: Owner
    template:
      engineVersion: v2
      templateFrom:
        - configMap:
            name: thebes-mc-paper-global-tpl
            items:
              - key: paper-global.yml
  data:
    - secretKey: forwarding_secret
      remoteRef:
        key: minecraft/velocity
        property: forwarding_secret
