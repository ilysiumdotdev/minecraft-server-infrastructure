apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mc-thebes
spec:
  serviceName: mc-thebes
  selector:
    matchLabels:
      app.kubernetes.io/name: mc-thebes
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mc-thebes
    spec:
      containers:
        - name: minecraft
          env:
            - name: PLUGINS_FILE
              value: "/extras/plugins.txt"
          envFrom:
            - configMapRef:
                name: mc-thebes-server-config
            - secretRef:
                name: mc-thebes-rcon-creds
          volumeMounts:
            - name: server-plugins
              mountPath: /extras/plugins.txt
              subPath: plugins.txt
            - name: minimotd-config
              mountPath: /plugins/MiniMOTD/main.conf
              subPath: main.conf
            - name: paper-global
              mountPath: /config/paper-global.yml
              subPath: paper-global.yml
      volumes:
        - name: server-plugins
          configMap:
            name: mc-thebes-server-plugins
        - name: minimotd-config
          configMap:
            name: mc-thebes-minimotd-config
        - name: paper-global
          secret:
            secretName: mc-thebes-paper-global
