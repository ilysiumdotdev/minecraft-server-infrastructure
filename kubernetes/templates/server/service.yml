apiVersion: v1
kind: Service
metadata:
  name: mc-server
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: mc-server
  ports:
    - name: minecraft
      port: 25565
    - name: rcon
      port: 25575
