apiVersion: v1
kind: Service
metadata:
  name: mc
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: minecraft
    app.kubernetes.io/instance: server
  ports:
    - name: minecraft
      port: 25565
    - name: rcon
      port: 25575
