apiVersion: v1
kind: Service
metadata:
  name: mc-server
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: mc-server
  ports:
    - name: game
      port: 25565
    - name: rcon
      port: 25575
