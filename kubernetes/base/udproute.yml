apiVersion: gateway.networking.k8s.io/v1alpha2
kind: UDPRoute
metadata:
  name: minecraft-bedrock-route
  namespace: minecraft
spec:
  parentRefs:
    - name: perimeter-cluster-gateway
      namespace: envoy-gateway-system
      sectionName: minecraft-bedrock
  rules:
    - backendRefs:
        - name: velocity
          port: 19132
