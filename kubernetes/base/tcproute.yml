apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: minecraft-route
  namespace: vector
spec:
  parentRefs:
    - name: perimeter-cluster-gateway
      namespace: envoy-gateway-system
      sectionName: tcp-minecraft
  rules:
    - backendRefs:
        - name: velocity
          port: 25565
